version: 0.1

phases:
  install:
    commands:
    - curl -fsSL https://deb.nodesource.com/setup_16.7.1 | sudo -E bash -
    - sudo apt-get install -y nodejs

  pre_build:
    commands:
      - npm install

  build:
    commands:
      - npm run build
